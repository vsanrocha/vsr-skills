{
  "name": "vsr-skills",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-registry.mjs",
    "validate": "node scripts/validate-catalog.mjs",
    "check-security": "node scripts/check-security.mjs",
    "check": "npm run build && npm run validate && npm run check-security"
  }
}
