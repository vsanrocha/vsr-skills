{
  "name": "vsr-skills",
  "version": "1.0.0",
  "private": true,
  "packageManager": "bun@1",
  "scripts": {
    "build": "bun run scripts/build-registry.mjs",
    "validate": "bun run scripts/validate-catalog.mjs",
    "check-security": "bun run scripts/check-security.mjs",
    "check": "bun run build && bun run validate && bun run check-security"
  }
}
