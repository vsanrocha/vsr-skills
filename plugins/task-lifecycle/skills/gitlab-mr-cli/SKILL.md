---
name: gitlab-mr-cli
description: Alias for /ship — creates a GitLab Merge Request for the current branch. Use /ship for the full multi-platform flow.
---

This skill is an alias for `/ship`.

Run `/ship` to create a Pull Request or Merge Request. It auto-detects whether the remote is GitHub or GitLab and uses the appropriate CLI.

```
/ship
```

See the `/ship` skill for full documentation and options.
