{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'Lint on save: configure seu linter aqui (ex: npm run lint:fix)'"
          }
        ]
      }
    ]
  }
}
